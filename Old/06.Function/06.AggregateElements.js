function aggregateElements(input) {
    let sum = function (firstNum, secondNum) {
        return firstNum + secondNum;
    }

    let sumInverse = function (firstNum, secondNum) {
        return firstNum + 1 / secondNum;
    }

    let concatenate = function (input) {
        
    }
}